
@{
    ViewBag.Title = "Khoa";
    ViewData["page"] = "Faculties list";
    ViewData["icon"] = "fa fa-sitemap fa-fw";
    ViewData["url"] = "/admin/room";
    Layout = "~/Areas/admin/Views/Shared/_Layout.cshtml";
}

<div class="row">
   
    <div class="col-md-2 col-md-offset-10 text-right">
        <button class="btn btn-success" data-toggle="modal" data-target="#facultyModal">
            <i class="fa fa-plus" aria-hidden="true"></i>
            Thêm khoa mới
        </button>
    </div>
</div>


<div class="table-responsive">
    <table class="table table-hover-color">
        <thead>
            <tr>
                <th>#</th>
                <th>Kí hiệu</th>
                <th>Tên</th>                
                <th></th>
            </tr>
        </thead>
        <tbody id="tblFaculties">  </tbody>
    </table>
</div>

<!-- Modal -->
<div id="facultyModal" class="modal fade" role="dialog">
    <div class="modal-dialog">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title" id="modalTitle">Tạo mới khoa</h4>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div class="col-md-12 form-group">
                        <label>Kí hiệu</label>
                        <input type="text" class="form-control" id="txtAcronym" placeholder="Faculty acronym" />
                    </div>                   
                </div>
                <div class="row">
                    <div class="col-md-12 form-group">
                        <label>Tên khoa</label>
                        <input type="text" class="form-control" id="txtName" placeholder="Faculty name"/>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-success" id="btnSubmit">Hoàn tất</button>
                <button type="button" class="btn btn-default" data-dismiss="modal">Đóng</button>
            </div>
        </div>

    </div>
</div>



@section scripts{        
<script src="~/Assets/admin/faculty.js"></script>
}

